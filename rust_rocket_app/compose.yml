services:
  db:
    container_name: rust_db
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres_db_rust
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: {}
